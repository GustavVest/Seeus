# ===========================================
# Seeus Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM）
openai>=1.0.0

# Legacy deps removed for production deploy: zep-cloud, camel-oasis, camel-ai.
# These were used by the original MiroFish simulation features (now dormant
# blueprints). Removing them avoids Python-version conflicts on Railway and
# keeps the runtime image / memory footprint small.

# ============= 文件处理 =============
PyMuPDF>=1.24.0
# 编码检测（支持非UTF-8编码的文本文件）
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# ============= Production WSGI =============
# Used by Railway / any PaaS to serve Flask in production.
gunicorn>=21.2.0

# ============= Image handling =============
# Pillow is used by the mockup placeholder + image-edit byte handling.
Pillow>=10.0.0

# ============= Email / lead capture =============
# Resend powers the free-checklist email and lead list. Falls back to
# silent in-app capture if RESEND_API_KEY is not set.
resend>=2.0.0
